import { describe, it } from 'mocha'
import * as assert from 'assert'


describe('User api', () => {

    it('User test', async () => {
        return Promise.resolve(assert.strictEqual('test', 'test'))
    })

})
